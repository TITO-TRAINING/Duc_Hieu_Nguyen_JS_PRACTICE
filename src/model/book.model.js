class Book {
  constructor({ id, title, author, category, status, number, price }) {
    this.id = id;
    this.title = title;
    this.author = author;
    this.category = category;
    this.status = status;
    this.number = number;
    this.price = price;
  }
}

export default Book;
